return {
    'nvim-neo-tree/neo-tree.nvim',
    enabled = false,
    opts = function()
        -- TODO: copy from my other neo-tree file.
        local opts = {

        }
        return opts
    end
}
